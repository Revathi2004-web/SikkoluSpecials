name: Build and Deploy
on:
  push:
    branches: [main]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Bun 🚀
        uses: oven-sh/setup-bun@v1

      - name: Install Dependencies 🔧
        run: bun install

      - name: Build 🏗️
        run: bun run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # OnSpace AI ప్రాజెక్ట్స్ కి సాధారణంగా ఇది 'dist' అని ఉండాలి
